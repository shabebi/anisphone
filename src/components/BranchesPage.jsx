import { useEffect, useState } from "react";
import "./BranchesPage.css";
const API="http://localhost:5000/api/v1";
export default function BranchesPage({language="en"}){const ar=language==="ar",[branches,setBranches]=useState([]);useEffect(()=>{fetch(`${API}/branches`).then(r=>r.json()).then(x=>setBranches(x.data||[])).catch(console.error)},[]);return <section className="branches-page" dir={ar?"rtl":"ltr"}><div className="branches-page-hero"><span>{ar?"فروعنا":"OUR LOCATIONS"}</span><h1>{ar?"تجربة أنيس فون، بالقرب منك":"The Anis Phone experience, near you"}</h1><p>{ar?"زرنا لتجربة الأجهزة والحصول على مساعدة متخصصة.":"Visit us to experience the latest devices and receive expert help."}</p></div><div className="branches-page-grid">{branches.map(b=><article className="branch-location-card" key={b.id}><div className="branch-pin">⌖</div><h2>{ar?b.name_ar:b.name_en}</h2><p>{ar?b.address_ar:b.address_en}</p>{b.phone&&<a href={`tel:${b.phone}`}>{b.phone}</a>}{b.map_url&&<a className="branch-map" href={b.map_url} target="_blank" rel="noreferrer">{ar?"عرض على الخريطة":"Open map"} →</a>}</article>)}</div></section>}
